import { hCode } from '../../../helpers/h-code';
import { module, test } from 'qunit';

module('Unit | Helper | h code');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = hCode(42);
  assert.ok(result);
});
